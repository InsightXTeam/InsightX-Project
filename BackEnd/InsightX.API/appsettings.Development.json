{
  "ConnectionStrings": {
    "DefaultConnection": "Data Source=(localdb)\\MSSQLLocalDB;Initial Catalog=InsightX;Integrated Security=True;Connect Timeout=30;Encrypt=True;Trust Server Certificate=False;Application Intent=ReadWrite;Multi Subnet Failover=False;Command Timeout=30"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Jwt": {
    "_comment": "Development-only JWT key. Override via dotnet user-secrets locally (e.g. dotnet user-secrets set 'Jwt:Key' '<value>').",
    "Key": "CHANGE_ME",
    "Issuer": "InsightX",
    "Audience": "InsightX",
    "AccessTokenExpiryMinutes": 15,
    "RefreshTokenExpiryDays": 7
  },
  "SuperAdmin": {
    "_comment": "Development-only seed credentials. Override via dotnet user-secrets locally.",
    "Email": "CHANGE_ME",
    "Password": "CHANGE_ME"
  }
}

